import 'package:flutter/material.dart';

class AppointmentsBox
    extends StatelessWidget {
  final String? elevated;
  final IconData? icon;
  final double? radius;
  final double? width;
  final double? height;
   ({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(width: 163,height: 125,decoration: BoxDecoration(borderRadius: BorderRadius.circular(10),
      border: Border.all(width: 1,color: Color(0xFFE31C31),),),child: ,);
  }
}
